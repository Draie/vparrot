{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ image.nom }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ image.createdAt ? image.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
             <td><img class="img-thumbnail" src="{{ asset('uploads/photo/' ~ image.photo) }}" alt="voiture photo"></td>
        </tbody>
    </table>

    <a href="{{ path('app_image_gest_index') }}">back to list</a>

    <a href="{{ path('app_image_gest_edit', {'id': image.id}) }}">edit</a>

    {{ include('image_gest/_delete_form.html.twig') }}
{% endblock %}

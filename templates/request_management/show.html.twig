{% extends 'base.html.twig' %}

{% block title %}RequestManagement{% endblock %}

{% block body %}
    <h1>RequestManagement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ request_management.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ request_management.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ request_management.Prenom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ request_management.mail }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ request_management.phoneNumber }}</td>
            </tr>
            <tr>
                <th>RequestObject</th>
                <td>{{ request_management.RequestObject }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ request_management.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_request_management_index') }}">back to list</a>

    <a href="{{ path('app_request_management_edit', {'id': request_management.id}) }}">edit</a>

    {{ include('request_management/_delete_form.html.twig') }}
{% endblock %}

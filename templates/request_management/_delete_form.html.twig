<form method="post" action="{{ path('app_request_management_delete', {'id': request_management.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request_management.id) }}">
    <button class="btn">Supprimer</button>
</form>

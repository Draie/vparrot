    
    {% block stylesheets %}
            <link href="{{ asset('css/counter.css') }}" rel="stylesheet" />
        {% endblock %}
    
     <div class="autoCounter">
                     <div class"count">
                        <h1>{{ count }}</h1>
                      </div>
                      <h2>Vehicule en vente</h2>
       </div>
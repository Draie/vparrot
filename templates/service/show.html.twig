{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}
  {% block stylesheets %}
            <link href="{{ asset('css/feedback.css') }}" rel="stylesheet" />
        {% endblock %}

{% block body %}


<div class="formContent">
    <h1>Service / {{service.id}}</h1>
    <table class="feedbackShow">
        <tbody>
        
            <tr class="rangeTitle">
                <th  class="range">Nom du service</th>
                <td  class="range">{{ service.Title }}</td>
            </tr>
            <tr>
                <th  class="rangeTitle">Description </th>
                <td  class="range">{{ service.Description }}</td>
            </tr>
        </tbody>
    </table>


      <a class="button" href="{{ path('app_service_index') }}">Retour à la liste</a>

    <a  class="button2"href="{{ path('app_service_edit', {'id': service.id}) }}">Modifier</a>

    {{ include('service/_delete_form.html.twig') }}

    </div>
  

{% endblock %}

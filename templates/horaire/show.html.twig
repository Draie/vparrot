{% extends 'base.html.twig' %}
  {% block stylesheets %}
            <link href="{{ asset('css/feedback.css') }}" rel="stylesheet" />
        {% endblock %}

{% block title %}Horaire Gestion{% endblock %}

{% block body %}

<div class="formContent">
  <h1> {{ horaire.Jour }}</h1>
    <table class="feedbackShow">
        <tbody>
        
            <tr>
                <th class="rangeTitle">Jour</th>
                <td>{{ horaire.Jour }}</td>
            </tr>
            <tr>
                <th class="rangeTitle">A partir de</th>
                <td>{{ horaire.morningStartTime ? horaire.morningStartTime|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th class="rangeTitle">A </th>
                <td>{{ horaire.morningEndTime ? horaire.morningEndTime|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th class="rangeTitle"> Et de </th>
                <td>{{ horaire.afterStartTime ? horaire.afterStartTime|date(' H:i') : '' }}</td>
            </tr>
            <tr>
                <th class="rangeTitle"> A </th>
                <td>{{ horaire.afterEndTime ? horaire.afterEndTime|date('H:i') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="button"href="{{ path('app_horaire_index') }}">Retour à la liste</a>

    <a class="button2"href="{{ path('app_horaire_edit', {'id': horaire.id}) }}">Modifier</a>

    {{ include('horaire/_delete_form.html.twig') }}
{% endblock %}
{% block footer %}
{% endblock %}